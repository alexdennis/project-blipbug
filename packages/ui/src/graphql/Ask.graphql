query Ask(
  $book: String!
  $character: String!
  $question: String!
  $audience: String
) {
  ask(
    book: $book
    character: $character
    question: $question
    audience: $audience
  ) {
    answer
  }
}
